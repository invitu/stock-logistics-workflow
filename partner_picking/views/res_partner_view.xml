<?xml version="1.0" encoding="utf-8" ?>
<odoo>
    <record id="act_partner_stock_picking" model="ir.actions.act_window">
        <field name="res_model">stock.picking</field>
        <field name="name">Pickings</field>
        <field name="view_mode">list,form,calendar,graph</field>
        <field name="domain">[("partner_id", "child_of", active_ids)]</field>
        <field
            name="context"
        >{'search_default_partner_id': active_id, 'default_partner_id': active_id}</field>
    </record>
    <record model="ir.ui.view" id="view_partner_form_pickings">
        <field name="name">Partner Form with pickings</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_partner_form" />
        <field name="arch" type="xml">
            <div name="button_box" position="inside">
                <button
                    name="%(partner_picking.act_partner_stock_picking)d"
                    type="action"
                    class="oe_stat_button oe_inline"
                    invisible="stock_picking_count == 0"
                    groups="stock.group_stock_user"
                    icon="fa-truck"
                >
                    <field name="stock_picking_count" widget="statinfo" string="Delivery" />
                </button>
            </div>
        </field>
    </record>
</odoo>
